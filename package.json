{
  "name": "coda-chat-engine-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Coda Chat Engine — holds Anthropic API key server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
